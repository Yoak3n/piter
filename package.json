{
  "name": "piter",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:all": "pnpm build:chat && pnpm dev",
    "build": "vue-tsc --noEmit && vite build",
    "build:chat": "cd chat && pnpm build",
    "build:all": "pnpm build && pnpm build:chat",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@piter/ui": "workspace:*",
    "@tauri-apps/api": "^2",
    "echarts": "^6.1.0",
    "lucide-vue-next": "^1.0.0",
    "marked": "^18.0.5",
    "vue": "^3.5.13",
    "vue-echarts": "^8.0.1",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  }
}
